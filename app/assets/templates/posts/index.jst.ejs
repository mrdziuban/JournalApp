<ul>
  <% _(posts.models).each(function (post) { %>
    <li class="post">
      <a href="#/posts/<%= post.id %>">
        <%= post.get("title") %>
      </a>
      <button id="<%= post.id %>" data-id="<%= post.id %>" class="delButton">Delete</button>
    </li>
  <% }); %>
</ul>